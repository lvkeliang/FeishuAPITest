问题概述：JSON反斜杠转义异常

在使用Jackson处理JSON数据时，反斜杠转义字符（如"\n"、"\""）可能导致序列化或反序列化失败。这种问题通常出现在API传输中，服务端返回的JSON字符串被额外添加了转斜杠，客户端解析时无法正确识别。

以下是常见场景：

服务端返回的JSON字符串中包含多余的转义符。
客户端解析时因未正确处理转义符而抛出异常。
双方编码规范不一致导致数据格式错误。
